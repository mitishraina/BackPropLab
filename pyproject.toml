[project]
name = "backproplab"
version = "0.1.0"
description = "Back Propogation to Classics"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "pandas>=2.3.3",
    "scikit-learn>=1.7.2",
]
